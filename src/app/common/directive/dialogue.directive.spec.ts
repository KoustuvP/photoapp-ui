import { DialogueDirective } from './dialogue.directive';

describe('DialogueDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogueDirective();
    expect(directive).toBeTruthy();
  });
});
